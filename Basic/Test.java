package Basic;

public class Test {

    public static void main(String args[]) {
        //1)
        // String s1 = "abc";
        // String s2 = "abc";

        // System.out.println("s1 == s2 is:" + s1 == s2);
        //2)
//          String s3 = "JournalDev";
// int start = 1;
// char end = 5;
// System.out.println(s3.substring(start, end));
//3)
// HashSet shortSet = new HashSet();
//  for (short i = 0; i < 100; i++) {
//     shortSet.add(i);//this adds elements of datatype short
//     shortSet.remove(i - 1);//this is trying to remove elements of integer datatype which means nothing is happening so the size remains unchanged
// }
// System.out.println(shortSet.size());
//4)
// try {
//  if (flag) {
//     while (true) {
//      }//if flag is true then this loops runs infinite times giving no output
//     } else {
//      System.exit(1);//if flag false it exits the JVM so doesnt reach finally 
//     }
// } finally {
//     System.out.println("In Finally");//in both cases finally is not triggered and hence no output in both cases
// }
//5)
// String str = null;
// String str1="abc";

// System.out.println(str1.equals("abc") | str.equals(null));
// String x = "abc";
// String y = "abc";

// x.concat(y);

// System.out.print(x);//abc- since x and y are poinitn gto same string pool and strings are immutable 
   
    //  int x = 10 * 10 - 10;
     
    //  System.out.println(x);

    }
}
